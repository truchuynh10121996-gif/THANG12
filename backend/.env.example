# ===========================================
# BACKEND ENVIRONMENT CONFIGURATION
# ===========================================
# Copy file này thành .env và điền các giá trị

# Server Configuration
PORT=5000
NODE_ENV=development

# ===========================================
# GOOGLE CLOUD TEXT-TO-SPEECH (TTS)
# ===========================================
# Đường dẫn đến file credentials JSON từ Google Cloud
# Hướng dẫn tạo: https://cloud.google.com/text-to-speech/docs/before-you-begin
#
# Bước 1: Tạo project tại https://console.cloud.google.com/
# Bước 2: Bật Cloud Text-to-Speech API
# Bước 3: Tạo Service Account và download file JSON
# Bước 4: Đặt file JSON vào thư mục backend và cập nhật đường dẫn bên dưới

GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# ===========================================
# GOOGLE CLOUD SPEECH-TO-TEXT (STT) - Optional
# ===========================================
# Sử dụng chung credentials với TTS ở trên
# Chỉ cần bật thêm Cloud Speech-to-Text API trong Google Cloud Console
